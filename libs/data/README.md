This compatibility library exposes shared task/auth contracts under the challenge-required `libs/data` path.

The implementation re-exports the existing shared contracts from the backend task domain.
