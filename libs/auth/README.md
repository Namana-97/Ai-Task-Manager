This compatibility library exposes reusable RBAC/auth decorators and permission utilities under the challenge-required `libs/auth` path.

The implementation re-exports the working auth layer from `apps/api-ai/src/auth`.
