This compatibility wrapper exposes the secure NestJS backend under the challenge-required `apps/api` path.

The live implementation remains in `apps/api-ai` to preserve the existing AI-integrated backend without refactoring.
