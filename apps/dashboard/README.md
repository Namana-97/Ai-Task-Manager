This compatibility wrapper exposes the Angular frontend under the challenge-required `apps/dashboard` path.

The live implementation remains in `apps/shell` to preserve the existing UI and AI integration without refactoring.
